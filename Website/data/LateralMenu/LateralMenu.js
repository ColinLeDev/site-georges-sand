/* Mettre la largeur du lat menu à 250px (ouvrir) */
function openNavMenu() {
    document.getElementById("LatMenu").style.width = "250px";
  }
  
  /* Mettre la largeur à 0 (fermer du coup) */
  function closeNavMenu() {
    document.getElementById("LatMenu").style.width = "0";
  }